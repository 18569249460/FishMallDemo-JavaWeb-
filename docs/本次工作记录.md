# 本次工作记录

日期：2026-05-08

## 工作目标

根据《水产品店在线销售管理系统项目计划书》，将刚创建的项目补齐为可继续开发的前后端分离项目骨架，重点完成依赖、目录、配置、数据库脚本和基础代码分层。

## 已完成内容

### 0. 本次补充：中文注释说明

- 为 `pom.xml` 中后端依赖补充了逐项中文用途说明。
- 为 Spring、MyBatis、Web、日志、数据库脚本和 Mapper XML 补充了中文配置说明。
- 为 Java 包、关键类、Controller、Service、Mapper、实体和 DTO 补充了中文注释。
- 为前端入口、路由、Axios、状态管理和 Vite 配置补充了中文注释。
- 由于 `package.json` 不能写注释，已新增 `docs/前端依赖说明.md` 说明前端依赖用途。

### 1. 后端 Maven 与 SSM 基础工程

- 将项目调整为 `war` 包工程，适配计划书中的 Tomcat 8 + Spring MVC 路线。
- 将原先偏 Tomcat 10 的 Spring 6 / Jakarta Servlet 依赖调整为 Spring 5.3 + `javax.servlet` 体系。
- 补齐后端核心依赖：
  - Spring Context / Spring MVC / Spring JDBC / Spring TX
  - MyBatis / MyBatis Spring
  - MySQL Connector / Druid
  - Jackson / JSR310 时间模块
  - Hibernate Validator / Validation API
  - JJWT
  - Log4j2
  - Lombok
  - JUnit / Spring Test

### 2. 后端目录与配置文件

- 创建 `src/main/webapp/WEB-INF/web.xml`，配置 Spring 根容器、DispatcherServlet 和 UTF-8 编码过滤器。
- 创建 Spring 配置：
  - `src/main/resources/spring/applicationContext.xml`
  - `src/main/resources/spring/spring-mvc.xml`
- 创建 MyBatis 配置：
  - `src/main/resources/mybatis/mybatis-config.xml`
  - `src/main/resources/mapper/*.xml`
- 创建运行配置：
  - `src/main/resources/db.properties`
  - `src/main/resources/log4j2.xml`
- 创建数据库初始化脚本：
  - `src/main/resources/sql/schema.sql`

### 3. 后端代码骨架

- 补齐基础包结构：`common`、`config`、`controller`、`dto`、`entity`、`interceptor`、`mapper`、`service`、`service.impl`、`util`。
- 新增统一响应与异常处理：
  - `ApiResponse`
  - `ResultCode`
  - `BusinessException`
  - `GlobalExceptionHandler`
- 新增业务枚举：
  - `UserRole`
  - `UserStatus`
  - `OrderStatus`
- 新增 JWT、明文密码处理、订单号、手机号脱敏工具类。
- 新增登录拦截器 `AuthInterceptor`，支持 Token 校验和 `/api/admin/**` 管理员权限判断。
- 新增 6 张核心表对应实体：
  - `User`
  - `Category`
  - `Product`
  - `Cart`
  - `Order`
  - `OrderItem`
- 新增主要请求/响应 DTO。
- 新增 Mapper 接口与 XML 映射文件。
- 新增 Controller 路由骨架，覆盖计划书中的主要 API。
- 新增 Service 接口与实现类骨架。除仪表盘统计外，业务方法当前以 `501 not implemented` 明确占位，后续可逐模块补逻辑。

### 4. 前端 Vue 3 项目骨架

- 新增 `frontend/` 前端工程。
- 创建 Vite + Vue 3 基础配置。
- 补齐前端依赖：
  - Vue 3
  - Vue Router
  - Pinia
  - Axios
  - Element Plus
  - Element Plus Icons
  - Vite
- 新增前端基础目录：
  - `frontend/src/api`
  - `frontend/src/assets`
  - `frontend/src/components`
  - `frontend/src/router`
  - `frontend/src/stores`
  - `frontend/src/views/user`
  - `frontend/src/views/admin`
- 新增基础路由、用户状态存储、Axios 封装和页面占位。

### 5. 忽略规则

- 更新 `.gitignore`，忽略：
  - 后端构建产物 `target/`
  - 日志文件
  - 前端 `node_modules/`
  - 前端构建产物 `frontend/dist/`
  - Vite 缓存

## 验证结果

- 后端：执行 `mvn -q -DskipTests package`，通过。
- 前端：执行 `npm install`，依赖安装成功，并生成 `frontend/package-lock.json`。
- 前端：执行 `npm run build`，通过。
- Vite 构建时提示单个 chunk 超过 500 kB，原因是当前 Element Plus 全量引入；这不影响骨架使用，后续可通过按需引入或路由懒加载优化。

## 当前状态说明

- 项目已经具备继续开发的基础结构。
- 后端接口路径已按计划书挂好，认证模块已实现注册、登录、JWT 返回和当前用户信息查询。
- 数据库脚本与 Mapper 已准备好，后续实现 Service 时可以直接接入。
- 前端已有路由、页面目录和接口封装，登录/注册页已接入后端认证接口。
- 当前目录不是 Git 仓库，因此本次没有生成 Git 提交记录。

## 认证模块实现记录

- 实现 `AuthServiceImpl.login`：按手机号查询用户，校验明文密码，拒绝禁用账号，成功后返回 JWT 和用户信息。
- 实现 `AuthServiceImpl.register`：校验手机号唯一，创建普通启用用户，密码按当前项目要求明文保存，注册成功后自动返回 JWT。
- 实现 `UserServiceImpl.getCurrentUser`：根据 Token 中的用户 ID 查询当前用户，返回不包含密码的 `UserDTO`。
- 新增 `UserDtoConverter`：统一转换用户实体，避免向前端暴露 `password` 字段，并生成脱敏手机号。
- 增强 `AuthInterceptor`：受保护接口会校验 Token、用户是否存在、用户是否启用；管理端接口额外校验管理员角色。
- 前端补充 `authApi.me`、用户 Store 登录/注册/查询当前用户动作，以及登录/注册页面交互。

## 商品分类与商品浏览实现记录

- 实现 `CategoryServiceImpl`：分类列表、新增、编辑、删除；删除前会判断分类下是否存在商品。
- 实现 `ProductServiceImpl`：商品列表、热门商品、商品详情、新增、编辑、删除；查询支持分类筛选和商品名称模糊搜索。
- 商品新增/编辑会校验分类是否存在，商品详情和不存在的分类/商品会返回明确错误。
- 初始化脚本 `schema.sql` 新增示例商品数据，方便首页热门商品、商品列表和商品详情页联调。
- 新增前端商品卡片组件 `ProductCard.vue`，复用于首页和商品列表。
- 首页已接入 `/api/products/hot`，展示热门商品。
- 商品分类页已接入 `/api/categories` 和 `/api/products`，支持分类切换和关键字搜索。
- 商品详情页已接入 `/api/products/{id}`，展示图片、分类、价格、库存、规格、销量和描述。

## 页面测试记录

- 后端构建：`mvn -q -DskipTests package` 通过。
- 前端构建：`npm run build` 通过。
- 已启动前端开发服务器，访问地址为 `http://localhost:5173`。
- 本地 HTTP 检查 `http://localhost:5173` 返回 200。
- 当前 Codex 会话未发现可用的 in-app browser 后端，因此无法在内置浏览器中截图/点击测试；可在本机浏览器直接打开上述地址测试。
- 页面商品数据依赖后端部署到 `http://localhost:8080/fishMall`，并需要先执行 `schema.sql` 初始化数据库。

## 页面测试问题修复记录

- 顶部导航新增“登录 / 注册”入口，登录后显示用户昵称下拉菜单。
- 管理后台菜单仅管理员登录后显示。
- 购物车、订单确认、个人中心新增前端路由登录拦截，未登录会跳转 `/login`。
- 管理端路由新增管理员角色拦截，非管理员不能进入 `/admin/**`。
- 登录成功后优先回到原本想访问的页面，例如从购物车跳登录后会回到购物车。
- 页面刷新后会调用 `/api/users/me` 校验 Token 是否仍有效，失效时清理登录态并跳转登录页。
- 个人中心页补充用户信息展示和退出登录功能。
- 管理端首页接入真实 `/api/admin/dashboard` 统计数据。
- 新增 `docs/测试数据.sql`，用于手动插入普通用户、管理员、分类和商品测试数据。

## 购物车与订单实现记录

- 实现 `CartServiceImpl`：购物车列表、加入购物车、修改数量、删除购物车项。
- 加入购物车时会校验商品存在、未售罄和数量不超过库存；已有商品会累加数量。
- 修改和删除购物车项时会校验当前用户归属，避免越权操作他人购物车。
- 实现 `OrderServiceImpl.createOrder`，并使用 `@Transactional` 包住完整下单流程。
- 下单时后端会重新读取购物车项，按数据库中的当前商品价格计算总金额。
- 下单事务中依次完成创建订单、创建订单商品快照、条件扣减库存、增加销量、清理购物车。
- 扣库存使用 `stock >= quantity` 条件更新，防止并发下单导致超卖。
- 实现用户订单列表、订单详情、确认收货；确认收货只允许“待收货 -> 已完成”。
- 实现管理端订单列表、订单详情和发货；发货只允许“待发货 -> 待收货”。
- 商品详情页已接入加入购物车，未登录会跳转登录页。
- 购物车页已接入列表、数量修改、删除、勾选结算和合计金额。
- 订单确认页已接入勾选商品展示、收货信息填写和提交订单。
- 个人中心已接入我的订单列表和确认收货。
- 后台订单页已接入订单列表、状态筛选、详情查看和发货操作。
- `docs/测试数据.sql` 已调整为可重复执行，会先清理测试账号相关购物车和订单。

## 管理端 CRUD 实现记录

- 实现 `UserServiceImpl.listUsers`，管理端用户列表返回 `UserDTO`，不暴露密码字段。
- 实现 `UserServiceImpl.updateStatus`，支持启用/禁用普通用户，管理员账号禁止被页面误禁用。
- 分类管理页已接入分类列表、新增、编辑、删除。
- 删除分类时前端会二次确认；后端会拒绝删除仍有关联商品的分类。
- 商品管理页已接入商品列表、名称搜索、新增、编辑、删除。
- 商品新增/编辑弹窗包含名称、分类、价格、库存、规格、图片 URL、描述。
- 商品删除前会二次确认；历史订单通过订单商品快照保留原商品信息。
- 用户管理页已接入用户列表、角色/状态展示、普通用户启用禁用。
- 所有管理端 CRUD 页面都保留中文注释，说明加载、保存、删除和状态切换逻辑。
- 本轮验证后，`src/main/java/com/fishshop/service/impl` 中已无 `NOT_IMPLEMENTED` 占位。

## 前端表单校验与空状态优化记录

- 登录/注册页改为 Element Plus 表单校验，手机号、密码、昵称均使用中文校验提示；手机号格式错误会提示“请输入正确的手机格式”。
- 后端登录、注册、收货电话 DTO 的手机号正则同步收紧到 `^1[3-9]\d{9}$`，与前端校验保持一致。
- 订单确认页补充收货人、联系电话、详细地址校验；空结算商品状态增加“返回购物车”按钮。
- 管理端分类新增/编辑弹窗补充分类名称必填与长度校验，并在分类列表为空时提供“新增分类”入口。
- 管理端商品新增/编辑弹窗补充商品名称、分类、价格、库存、规格和图片 URL 校验；商品列表无数据或搜索无结果时显示中文空状态。
- 管理端用户和订单列表补充中文空状态，空列表时提供刷新操作。
- 前台首页、商品列表、商品详情、购物车、个人中心补充更明确的中文空状态和返回/跳转按钮。
- Element Plus 全局语言切换为中文，避免组件内置提示、空数据和按钮文案出现英文。
- 前端路由拦截未登录、无后台权限时会给出中文提示。
- Axios 响应拦截器补充网络异常、超时、401、403、500 的中文兜底提示。
- 后端统一结果码从英文默认文案改为中文，兜底异常不再把英文异常信息直接返回给前端。
- 本轮验证：后端 `mvn -q -DskipTests package` 通过；前端 `npm run build` 通过，Vite 仅提示包体积较大。

## 优先修复项完成记录

- 修复前台导航高亮映射：商品详情页归属“商品分类”，订单确认页归属“购物车”，个人中心子页面归属“个人中心”。
- 管理端改为独立后台外壳：顶部管理员信息栏、左侧侧边栏、右侧主内容区、底部版权信息。
- 管理端侧边栏包含首页、账户管理、商品分类管理、商品管理、订单管理，并根据当前路由保持高亮。
- 后台首页补充统计卡片、横向数据概览图、待处理订单占比图和快捷入口，避免页面过空。
- 账户管理、分类管理、商品管理、订单管理统一为“页面标题 + 筛选栏 + 表格卡片 + 分页/空状态”的后台布局。
- 商品管理增加分类筛选和本地分页，清空筛选时不会向后端传空分类参数。
- 订单管理增加订单号/用户昵称本地搜索、状态筛选、分页和详情弹窗展示。
- 新增根目录 `README.md`，记录数据库准备、IDEA Tomcat 配置、前端启动、测试账号和常见问题。
- 本轮验证：后端 `mvn -q -DskipTests package` 通过；前端 `npm run build` 通过，Vite 仍仅提示包体积较大。

## 前端底部版权信息记录

- 用户端外壳新增底部版权栏，所有用户端页面统一展示“© 2026 水产品管理系统 版权所有 · ICP备案号：ICP备2026005188号”。
- 管理端底部版权栏补充 ICP 备案号，展示为“© 2026 水产品管理系统 管理后台 版权所有 · ICP备案号：ICP备2026005188号”。
- 版权栏位于页面底部，短页面也会贴近视口底部，长页面则跟随内容自然下移。
- 本轮验证：前端 `npm run build` 通过，Vite 仅提示包体积较大。

## 首页轮播与热门商品布局记录

- 首页 `home-hero` 改为 Element Plus 轮播图，数据来源为后端 `/api/products/hot` 返回的销量排序商品。
- 轮播图取销量最高且有图片的前三个商品，展示商品图片、名称、分类、销量和查看入口。
- 点击轮播图可直接进入对应商品详情页。
- 热门水产区域固定取前 8 个热门商品，桌面端按 4 列展示，形成一行四个、共两行的布局。
- 本轮新增代码已补充中文注释，说明轮播图数据来源、热门商品数量和点击跳转逻辑。
- 本轮验证：前端 `npm run build` 通过，Vite 仅提示包体积较大。

## 功能完善建议第二部分完成记录

- 商品管理：后台商品列表补充分类筛选、本地分页、库存状态标签和上下架标签。
- 商品表单：新增商品上下架状态、产地、保质期、储存方式、配送说明；图片支持 URL，也支持本地上传后保存访问地址。
- 前台商品：商品列表、热门商品和分类只展示启用/上架数据；商品详情补充产地、保质期、储存方式、配送说明。
- 分类管理：分类新增排序和启用状态；后台列表显示关联商品数量；删除提示中展示当前关联商品数量。
- 购物车：增加批量删除和失效商品清理；已下架、售罄或数量超过库存的商品不能结算。
- 订单：新增用户订单详情页 `/orders/:id`；支持待发货订单取消并回滚库存和销量；订单状态增加“已取消”。
- 发货：后台发货弹窗支持填写物流单号，用户端订单列表和订单详情显示物流信息。
- 用户中心：支持修改昵称和头像；新增常用收货地址管理；订单列表支持状态筛选和订单号搜索。
- 下单页：会加载用户常用地址，并自动带入默认地址，用户可一键选择地址。
- 管理端用户：继续采用禁用/启用作为软删除方案，避免硬删除影响历史订单。
- 数据库：`schema.sql` 已补充新字段和 `user_address` 表；新增 `docs/功能完善升级SQL.sql` 用于已有数据库平滑升级。
- 本轮验证：后端 `mvn -q -DskipTests package` 通过；前端 `npm run build` 通过，Vite 仅提示包体积较大。

## 细节问题修复记录

- 订单详情、个人中心订单列表、账户管理、商品分类管理、订单管理的时间统一显示为“2026年5月8日 19:20:46”这类中文格式。
- 个人中心头像改为本地图片上传：后端保存到 `/uploads/images/touxiang`，前端把返回的图片地址写入用户资料。
- 用户登录后，前台导航右侧由“登录 / 注册”按钮切换为圆形头像下拉入口。
- 注册表单新增“确认密码”，登录/注册框已改为页面居中展示。
- 管理员登录后默认进入 `/admin` 后台首页，避免落到前台首页。
- 后台首页“数据概览”改为近七天销量折线图；订单处理占比改用当天订单数据；已移除快捷入口 `quick-grid`。
- 商品分类管理和商品管理的操作栏新增启用/禁用、上架/下架快捷按钮。
- “全部订单管理”菜单和页面标题统一改为“订单管理”。
- 新增 `docs/分页测试商品分类数据.sql`，用于补充分类和商品数据以测试分页效果。
- 本轮验证：后端 `mvn -q -DskipTests package` 通过；前端 `npm run build` 通过，Vite 仅提示包体积较大。

## 前端细节体验第二轮修复记录

- 客户端商品分类页新增分页，每页 8 个商品；桌面端固定两行、每行 4 个商品。
- 客户端分类无商品时，左侧分类项和空状态都会显示中文提示。
- 商品详情页加入购物车后停留当前页面，只提示“已加入购物车”。
- 底部版权备案号已去掉“粤”字。
- 个人中心订单状态筛选清空后可恢复“全部状态”。
- 手机号格式校验文案统一为“请输入正确的手机格式”。
- 管理端首页近七天销量改为 SVG 折线图展示。
- 账户管理每页显示 8 个账户信息；商品分类管理每页 12 条并支持按分类名称、关联商品数切换升降序；商品管理每页 8 条。

## 管理员账户添加功能记录

- 普通注册入口继续只创建普通用户，避免公开页面直接注册管理员账号。
- 后端新增 `/api/admin/users/admins` 接口，该接口位于管理员权限拦截范围内，只有已登录管理员能调用。
- 新增 `AdminCreateRequest`，对管理员昵称、手机号和密码进行中文校验提示。
- `UserServiceImpl.createAdmin` 复用 `user` 表创建管理员，`role` 写入 `1`，密码按当前项目要求明文保存。
- 管理端“账户管理”页面新增“新增管理员”按钮和弹窗表单，包含昵称、手机号、密码、确认密码校验。
- 新增 `docs/管理员账户添加说明.md`，说明后台添加管理员和第一位管理员手动 SQL 初始化方式。
- 本轮验证：后端 `mvn -q -DskipTests package` 通过；前端 `npm run build` 通过，Vite 仅提示包体积较大。

## 图片目录分流记录

- 新增 `src/main/webapp/uploads/images` 下的图片目录，用于保存本地上传图片。
- 后端上传接口把头像保存到 `/uploads/images/touxiang`，商品图片保存到 `/uploads/images/shangpin`，文件名分别带 `avatar_`、`product_` 前缀。
- 新增 `/api/uploads/product` 商品图片上传接口，返回可直接写入商品 `image_url` 字段的访问地址。
- 前端商品管理页本地选择图片后改为上传到后端，不再转成 base64 写入数据库。
- 头像上传接口保留 `/api/uploads/avatar` 地址不变，只调整底层保存目录，减少前端改动。
- 本轮验证：后端 `mvn -q -DskipTests package` 通过；前端 `npm run build` 通过，Vite 仅提示包体积较大。

## 建议下一步

1. 按完整业务流程做一轮手动联调：注册/登录、加购、下单、后台发货、用户确认收货。
2. 后续可以继续补图片删除清理、订单详情页的独立路由，以及后台接口级分页。
